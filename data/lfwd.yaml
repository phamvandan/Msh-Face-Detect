# Train/val/test sets as
## 1) dir: path/to/imgs, 2) file: path/to/imgs.txt, or 3) list: [path/to/imgs1, path/to/imgs2, ..]


#path: ./datasets/maskedFace  # dataset root dir
train: ./datasets/lfwd/val  # train images (relative to 'path') 128 images
val: ./datasets/lfwd/val # val images (relative to 'path') 128 images
test: ./datasets/lfwd/test/images

# Classes
nc: 1  # number of classes
names: ['face']  # class names